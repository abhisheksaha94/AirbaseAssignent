
BaseTest.java
One can find all the global variables and their initialization in setUp method

Helper.java
One can find all required helper methods here

testFlow.properties
All the identifiers in this file so that if anything changes in the future,
one can fix that issue by changing the values at one place only

SampleTest.java
One can fine the automation for Use case as mentioned in the assignment.
TestNg is used to write the test and DataProvider annotation of TestNg
is used to run the same test on 3 different data set.
AfterTest Annotation is used to quite the browser once the tests are over.


NOTE:
Please change the path of Chrome Driver in testFlow.properties to match the local system version of chrome driver.

You can also see the Task 1 assignment under Task1 folder